-- Add billing columns to organizations
ALTER TABLE public.organizations
  ADD COLUMN IF NOT EXISTS payment_failure_count int DEFAULT 0,
  ADD COLUMN IF NOT EXISTS payment_first_failed_at timestamptz,
  ADD COLUMN IF NOT EXISTS cancelled_at timestamptz,
  ADD COLUMN IF NOT EXISTS data_retention_until timestamptz;
