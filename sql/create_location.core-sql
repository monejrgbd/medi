-- Private implementation
CREATE OR REPLACE FUNCTION private.create_location(
  p_caller_auth_uid uuid,
  p_org_id uuid,
  p_name text,
  p_address text DEFAULT NULL,
  p_specialty text DEFAULT NULL,
  p_operating_hours jsonb DEFAULT NULL
)
RETURNS jsonb
LANGUAGE plpgsql
VOLATILE
SECURITY DEFINER
SET search_path TO ''
AS $$
DECLARE
  v_location_id uuid;
  v_owner_id uuid;
BEGIN
  -- Verify caller is the org owner
  SELECT owner_id INTO v_owner_id
  FROM public.organizations
  WHERE id = p_org_id;

  IF v_owner_id IS NULL OR v_owner_id <> p_caller_auth_uid THEN
    RETURN jsonb_build_object('success', false, 'error', 'Only the organization owner can create locations');
  END IF;

  INSERT INTO public.locations (org_id, name, address, specialty, operating_hours)
  VALUES (p_org_id, p_name, p_address, p_specialty, p_operating_hours)
  RETURNING id INTO v_location_id;

  -- Set qr_code_url
  UPDATE public.locations
  SET qr_code_url = '/checkin/' || v_location_id
  WHERE id = v_location_id;

  RETURN jsonb_build_object('success', true, 'location_id', v_location_id, 'qr_code_url', '/checkin/' || v_location_id);
END;
$$;

-- Public wrapper
CREATE OR REPLACE FUNCTION public.create_location(
  p_org_id uuid,
  p_name text,
  p_address text DEFAULT NULL,
  p_specialty text DEFAULT NULL,
  p_operating_hours jsonb DEFAULT NULL
)
RETURNS jsonb
LANGUAGE plpgsql
VOLATILE
SECURITY DEFINER
SET search_path TO ''
AS $$
DECLARE
  v_auth_uid uuid := auth.uid();
BEGIN
  RETURN private.create_location(v_auth_uid, p_org_id, p_name, p_address, p_specialty, p_operating_hours);
END;
$$;

REVOKE EXECUTE ON FUNCTION public.create_location(uuid, text, text, text, jsonb) FROM anon;
GRANT EXECUTE ON FUNCTION public.create_location(uuid, text, text, text, jsonb) TO authenticated;
