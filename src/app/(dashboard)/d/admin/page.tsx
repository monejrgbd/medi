import AdminPremiumCodes from "@/components/admin/AdminPremiumCodes";

export const metadata = {
  title: "Premium Codes — Hilt Admin",
};

export default function AdminPage() {
  return <AdminPremiumCodes />;
}
